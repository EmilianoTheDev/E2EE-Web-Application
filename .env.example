# Client build variable used by Create React App.
# On Vercel, set this to the public HTTPS URL of the deployed Socket.IO server.
REACT_APP_SERVER_URL=http://localhost:3001

# Server variables used when running/deploying the Socket.IO server.
PORT=3001
CLIENT_ORIGIN=http://localhost:3000
